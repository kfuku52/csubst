name: Manual Dispatch Test

on:
  workflow_dispatch:

jobs:
  test-dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Print confirmation
        run: echo "✅ このワークフローは手動実行されています（workflow_dispatch が有効です）"
