name: Build wheels

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

jobs:
  wheels:
    name: ${{ matrix.name }} wheels
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Linux x86_64
            os: ubuntu-latest
            archs: x86_64
          - name: macOS x86_64
            os: macos-15-intel
            archs: x86_64
          - name: macOS arm64
            os: macos-14
            archs: arm64
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"

      - name: Install cibuildwheel
        run: python -m pip install --upgrade cibuildwheel

      - name: Build binary wheels
        env:
          CIBW_BUILD: "cp310-* cp311-* cp312-* cp313-* cp314-*"
          CIBW_ARCHS: ${{ matrix.archs }}
          CIBW_SKIP: "*-musllinux_*"
          CIBW_BEFORE_BUILD: "python -m pip install Cython numpy"
          CIBW_TEST_COMMAND: >-
            python -c "import csubst.combination_cy, csubst.omega_cy,
            csubst.parser_iqtree_cy, csubst.recoding_cy,
            csubst.substitution_cy, csubst.substitution_sparse_cy"
            && csubst --version
        run: python -m cibuildwheel --output-dir wheelhouse

      - name: Upload wheels
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: csubst-${{ matrix.os }}-${{ matrix.archs }}-wheels
          path: wheelhouse/*.whl
